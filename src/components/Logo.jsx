function Logo({ className }) {
  return (
    <div>
      <h1 className={`uppercase font-cinzel ${className}`}>
        snow<span className="text-brandGold">mac</span>
      </h1>
    </div>
  );
}

export default Logo;
